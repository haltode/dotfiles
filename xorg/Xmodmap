! Capslock is now the meta key (used mainly for i3wm)
! Shift+Capslock acts like the normal Capslock key
! Left super (cmd on mac) is now a control key
clear lock
clear control
clear mod4
add mod4 = Caps_Lock
add control = Super_L Control_L
keycode 66 = Control_L Caps_Lock NoSymbol NoSymbol

! Exchange cmd and alt key (so that cmd acts like right alt key, used as alt-gr
! for French keyboard layout)
keycode 108 = Super_R NoSymbol Super_R
keycode 134 = ISO_Level3_Shift NoSymbol ISO_Level3_Shift
